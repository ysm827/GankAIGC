# GankAIGC - 打包依赖
# 包含后端所有依赖 + PyInstaller

# FastAPI 后端依赖
fastapi==0.136.1
uvicorn[standard]==0.27.0
sqlalchemy==2.0.49
pydantic==2.10.3
pydantic-settings==2.14.1
python-multipart==0.0.28
python-dotenv==1.2.2
openai==1.10.0
httpx[socks]==0.27.0
httpx2==2.4.0
aiofiles==23.2.1
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
alembic==1.13.1
redis==5.0.1
aioredis==2.0.1
sse-starlette==3.0.3
pytest==9.0.3
pytest-asyncio==1.3.0
psycopg[binary]==3.2.6

# Word 格式化模块依赖
mistune>=3.0.0
python-docx>=1.1.0
markitdown[docx,pdf]>=0.1.3,<1.0.0
# Docling 会依赖 torch/torchvision；必须先锁 CPU-only 轮子，避免默认 CUDA 包拖垮 WSL/Docker Desktop。
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.1+cpu
torchvision==0.27.1+cpu
docling>=2.0.0,<3.0.0
lxml>=4.9.0

# 打包工具
pyinstaller==6.3.0
platformdirs>=4.2.0,<5

# PyInstaller 打包 Linux 需要显式包含 (pkg_resources 依赖)
jaraco.text
jaraco.functools
jaraco.context
# 朱雀微信扫码凭证捕获
playwright>=1.40.0
